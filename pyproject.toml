[tool.poetry]
name = "python-mood-tracker"
version = "0.1.0"
description = ""
authors = ["Liisa <liisa-marie.kettunen@helsinki.fi>"]
readme = "README.md"
packages = [{include = "python_mood_tracker"}]

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
